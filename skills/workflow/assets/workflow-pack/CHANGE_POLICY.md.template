# {{TITLE}}｜变更规则

## 版本

- 修正文案且不改变行为：patch。
- 改变默认值、门禁或条件分支：minor，并重跑相关案例。
- 改变成功契约、权限边界或产物兼容性：major，并重跑全部案例。

## 回归规则

任何改动前后都运行记录在 `WORKFLOW_SPEC.json.validation.commands` 中的命令。受保护行为失败时，状态立即降级，不得弱化断言来维持通过。

`DEPRECATED` 表示该包不再作为可执行默认版本：必须写明废弃原因和替代版本，`optimization.claim` 重置为 `UNASSESSED`，不得沿用原来的“已验证”或最短性措辞。替代版本按自己的定义哈希重新完成验证，不能继承旧证据。

## 新教训进入条件

只有真实失败、明确根因和可验证门禁同时存在时，才新增规则。偶发噪音、项目私有细节和已有规则覆盖的案例不进入全局工作流。
